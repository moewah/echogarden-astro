# Memos API（可选）
# 配置后构建时会自动拉取最新动态；留空则显示动态服务未配置的占位提示。
# MEMOS_ACCESS_TOKEN 可选：Memos 开启匿名公开访问时可不填（拉取不带 Authorization 头）；未开匿名时必填。
MEMOS_SITE_URL=https://memos.example.com
MEMOS_ACCESS_TOKEN=your_token_here

# Artalk 评论服务（可选）
# 配置后留言板块会延迟加载 Artalk；留空则显示评论服务未配置的占位提示
ARTALK_SERVER=https://artalk.example.com
# Artalk 站点名（在 Artalk 后台创建站点时设置）
ARTALK_SITE=demo

# GitHub 个人访问令牌（可选）
# 配置后项目展示板块使用认证请求拉取仓库，避免未认证 API 的速率限制（403）。
# 在 https://github.com/settings/tokens 创建，无需特殊 scope（公开仓库只读即可）。
GITHUB_TOKEN=your_token_here
